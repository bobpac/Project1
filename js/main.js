/*----- constants -----*/
/*----- state variables -----*/
/*----- cached elements  -----*/
const sqrs = document.getElementById('board')

/* event listeners */
sqrs.addEventListener('click',function(evt) {
  let sqrId = evt.target.id;
});

/*----- functions -----*/
